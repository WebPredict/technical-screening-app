<% provide(:title, 'Edit Category') %>
<div class="page-header">
  <h3>Edit Category</h3>
</div>

<div class="row">
  <div class="col-md-12">
    <%= form_for(@category) do |f| %>
      <%= render 'shared/error_messages', object: f.object %>

      <%= f.label :name %>
      <%= f.text_field :name, class: 'form-control' %>

      <div class="form-group">
        <div style="float:right">
          <%= f.submit "Update Category", class: "btn btn-primary btn-responsive" %>
          <span></span>
          <%= f.submit "Cancel", class: "btn btn-default btn-responsive" %>
      </div>
      </div>
          
    <% end %>
  </div>
</div>
<br>
