<div class="panel panel-default">
  <div class="panel-heading">
    <h3 class="panel-title">Job Listings<span style="float:right">
      <%= link_to jobs_path, class: "btn btn-xs btn-default" do %>
    <i class="glyphicon glyphicon-search"></i>
    <% end %></span></h3>
    </div>
  <div class="panel-body">
<% if @jobs && @jobs.any? %>
    <div class="table-responsive">
    <table class="table">
      <tr><th>Job Name</th><th>Description</th><th>Company</th><th>Created At</th><th>Actions</th></tr>
      <tbody>
      <%= render @jobs %>
      </tbody>
    </table>
    </div>
  <%= will_paginate @jobs %>
<% else %>
<strong>No job listings yet. <%= link_to "Add one!", new_job_path %></strong>
<% end %>
</div>
</div>
