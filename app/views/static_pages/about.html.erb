<% provide(:title, "About") %>
<div class="page-header">
<h1><%= image_tag("big-logo.png") %></h1>
</div>
<div class="row">
  <div class="col-md-12">
  <p class="center">
  TechScreen.net helps you set up screening tests for software development candidates. 
  Quickly create tests from a variety of technology topics and thousands of questions, or create your own. Easily manage your candidates' results.
  </p>
  </div>
</div>
<div class="row">
  <div class="col-md-4">
    <h3>Unlimited Tests</h3>
    <p>Create as many screening tests as you'd like, with as many questions as you'd like (as a premium member).</p>
  </div>
    <div class="col-md-4">
    <h3>Large Question Library</h3>
    <p>Quickly put together screening tests for popular technologies using our existing library of questions and answers.</p>
  </div>
  <div class="col-md-4">
    <h3>Manage Candidates</h3>
    <p>Invite as many interview candidates as you'd like to take your tests, score and manage the results (as a premium member).</p>
  </div>
</div>

<div class="row">
  <div class="col-md-12">
    <h1>Technical Interview Screening Questions Include:</h1>
  </div>
</div>
    
<div class="row">
  <div class="col-md-4">
    <ul>
    <% @categories_first.each do |category| %>
    <li><%= link_to category.name, questions_path(category_id: category) %></li>
    <% end %>
    </ul>
  </div>
    <div class="col-md-4">
    <ul>
    <% @categories_second.each do |category| %>
    <li><%= link_to category.name, questions_path(category_id: category) %></li>
    <% end %>
    </ul>
  </div>
  <div class="col-md-4">
    <ul>
    <% @categories_third.each do |category| %>
    <li><%= link_to category.name, questions_path(category_id: category) %></li>
    <% end %>
    </ul>
  </div>
</div>